// import { GET_ALL_CAMPAIGN_BY_SCREEN_ID_RESET } from "../constants/campaignConstants";
// import { GET_ALL_SCREENS_BY_USERID_RESET } from "../constants/screenDataConstant";

export const removeDispatchData = (dispatch: any) => {
  // dispatch({ type: GET_ALL_SCREENS_BY_USERID_RESET });
  // dispatch({ type: GET_ALL_CAMPAIGN_BY_SCREEN_ID_RESET });
};
